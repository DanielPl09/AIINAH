export const HEYGEN_CONFIG = {
  AVATAR_ID: 'Ann_Doctor_Standing2_public',
  // In a real app, the token is fetched via the API route, 
  // but we can put other config here.
  BASE_URL: 'https://api.heygen.com',
};
